//
//  DelegateExtension.swift
//  Meme Me
//
//  Created by Mark Lindamood on 4/20/16.
//  Copyright © 2016 udacity. All rights reserved.
//

/*import Foundation
import UIKit

// Copied from: http://stackoverflow.com/questions/26063039/uipickerview-loop-the-data
extension FontPickerViewController : UIPickerViewDelegate {
    class func numberOfComponentsInPickerView(pickerView: UIPickerView) -> Int {
        return 1
    }
    
    class func pickerView(pickerView: UIPickerView, numberOfRowsInComponent component: Int) -> Int {
        return pickerViewRows
    }
    
    // whenever the picker view comes to rest, we'll jump back to
    // the row with the current value that is closest to the middle
    class func pickerView(pickerView: UIPickerView, didSelectRow row: Int, inComponent component: Int) {
        let newRow = pickerViewMiddle + (row % pickerDataSource.count)
        pickerView.selectRow(newRow, inComponent: 0, animated: false)
        println("Resetting row to \(newRow)")
    }
    
}*/