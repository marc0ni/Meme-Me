//
//  DataSourceExtension.swift
//  Meme Me
//
//  Created by Mark Lindamood on 4/20/16.
//  Copyright © 2016 udacity. All rights reserved.
//

/*import Foundation
import UIKit

// Copied from: http://stackoverflow.com/questions/26063039/uipickerview-loop-the-data
extension FontPickerViewController : UIPickerViewDataSource {
    class func valueForRow(row: Int) -> Int {
        // the rows repeat every `pickerViewData.count` items
        return pickerDataSource[row % pickerDataSource.count]
    }
    
    class func rowForValue(value: Int) -> Int? {
        if let valueIndex = find(pickerDataSource, value) {
            return pickerViewMiddle + value
        }
        return nil
    }
    
    class func pickerView(pickerView: UIPickerView, titleForRow row: Int, forComponent component: Int) -> String! {
        return "\(valueForRow(row))"
    }
}*/